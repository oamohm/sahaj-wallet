# Which registered brand theme to render (see packages/branding-config).
# Leave unset to use the default "sahaj" theme.
VITE_ACTIVE_BRAND=sahaj

# Where the Vite dev server proxies /api requests during local development.
VITE_API_PROXY_TARGET=http://localhost:4000
